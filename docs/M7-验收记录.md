# ClauseGuard · M7 里程碑验收记录

| 项 | 内容 |
|---|---|
| 文档编号 | CG-M7-001 |
| 里程碑 | **M7 闭环演示 + 测试 + 截图 + 文档与交付整理**（设计 §14） |
| 状态 | ✅ 已完成并逐项验证 |
| 覆盖验收 | **AC19**（含 AC01–AC18 串链复核）、TS-20 |
| 验证日期 | 2026-09-18 |
| 依据 | `docs/SPEC.md` §13（AC01–AC19）、§14（TS-01…TS-20）；实战说明（提交格式） |
| 配套 | `docs/演示说明.md`、`docs/验收对照表.md`、`screenshots/` |

---

## 1. 范围与完成判据

| 编号 | 判据 | 结果 |
|---|---|---|
| DO-1 | AC19：从待办拉取到评论回写完整跑一遍 AP-001，全程无人工干预 | ✅ 35/35 项判据通过（§4.2） |
| DO-2 | 闭环各阶段数据齐全（落盘、解析、审查、入库、回写、日志） | ✅ 逐阶段回查库与磁盘，不只看接口返回 |
| DO-3 | TS-20 闭环端到端用例进入测试套件，可重复运行 | ✅ `tests/test_m7_e2e.py` 8 例 |
| DO-4 | 补齐 M2/M5 遗留截图（AC05 OCR、AC17 重试、AC19 闭环） | ✅ 见 §4.3 |
| DO-5 | 交付材料齐全：截图 + README + 演示说明 + AC 对照表 | ✅ |
| DO-6 | 一键可复现的演示脚本 | ✅ `scripts/demo_closed_loop.py` |
| DO-7 | 全量回归无失败 | ✅ 287 passed（§4.1） |
| DO-8 | 交付物不含 `.venv` / OCR 模型 / 密钥（实战说明的提交要求） | ✅ 见 §5 |

---

## 2. 交付物

| 文件 | 说明 |
|---|---|
| `tests/test_m7_e2e.py` | **新增** 8 例：AC19 九步闭环、日志齐备、AC02 去重、可追踪链、视图=库、快照缓存回归、降级、无命中模板 |
| `scripts/demo_closed_loop.py` | **新增**：对真实服务跑 `--reset / --retry / --all` 三段演示并逐条打印判据 |
| `backend/app/modules/approval/service.py` | **新增** `cache_approval_snapshot` / `sync_approval_snapshot`（修 §3.1 的缺陷） |
| `backend/app/tools/parser.py` | IF-14 进入解析前先执行"第 ② 步 详情"：缓存审批表单（尽力而为） |
| `frontend-or-client/src/views/TaskDetailView.vue` | 详情页改为"IF-02 取审批侧表单 + IF-12 取任务/附件"，IF-02 不可达时回落本系统缓存 |
| `frontend-or-client/src/api/{types,tasks}.ts` | 新增 `ApprovalDetail` 类型与 `getApprovalDetail`（IF-02 REST） |
| `frontend-or-client/src/views/TaskLayout.vue`、`src/composables/taskContext.ts` | 上下文增加 `approval` / `approvalError` |
| `tests/test_m5_logs.py` | 用例自足化（见 §3.2） |
| `screenshots/` | 12 张真实渲染截图（§4.3） |
| `docs/演示说明.md`、`docs/验收对照表.md` | **新增**：演示 Runbook 与 AC01–AC19 → 证据索引 |

---

## 3. 关键发现与实现决策

### 3.1 【R-01】AC19 用例当场抓出一个真实缺陷：审批表单数据从未落库

写端到端用例时断言"详情页应有表单数据"，结果是空的。追下去发现：

- `approval_tasks.contract_type` 与 `form_data_json` 这两列**在 M1–M6 期间从未被写入过**
  （`grep` 全仓只有定义与读取，没有任何 `INSERT/UPDATE`）；
- 根因是 SPEC/设计把"第 ② 步 详情"的实现理解成了"顺便取一下附件"：
  `ensure_attachments` 确实会调 IF-02，但拿到的 `contract_type` / `form_data` 被直接丢弃；
- 后果是 **FR-UI-02 的"表单数据"永远为空**，详情页的合同类型也一直是兜底值。

**修法**（最小且贴合设计原意）：设计 §7 对 `form_data_json` 的注解是
"审批表单字段（**PRD §6 第二步要求读取**）"，所以第 ② 步本来就该把详情落库。于是：

1. 新增 `cache_approval_snapshot()`：把 `contract_type` / `form_data` 写进任务行；
2. `parse_task`（承载第 ②③④⑤ 步）开头调用 `sync_approval_snapshot()`，
   **尽力而为**——审批系统不可达时只记 `warning`，不改变任务状态（与 LM-14"LLM 失败不阻塞"同一思路）；
3. 前端详情页对**尚未解析**的任务改从 IF-02（`/api/approvals/{id}`）取表单，
   该 REST 形态在 M1 就是"为了调用端详情页"而加的。

回归用例：`test_if12_form_data_is_cached_after_parse`、`test_approval_snapshot_is_best_effort`、
`test_task_row_matches_final_api_view`（视图=库）。

### 3.2 【R-02】两条 M5 用例依赖"别的用例留下的库状态"

把 `-k` 单独挑出来跑时，`test_m5_logs.py` 里有两条失败——**不是实现缺陷**：
它们默认"前面的用例已经解析过 AP-002 / 已给 AP-001 回过写"。全量跑时靠执行顺序侥幸通过，
单独跑就暴露了。已改为调用 `_run_loop()` 自行把链路推到目标阶段（每步都幂等），
用例之间不再有顺序依赖。

这与 M5-R-01 是同一类问题的两个层次：那次修的是"**跨轮**污染"（会话级清库），
这次修的是"**同轮内**用例间耦合"。

### 3.3 【R-03】闭环用例用 `NullLLMClient`，保证结论可复现

AC19 验证的是**闭环本身**，不是"某个 LLM 输出下的表现"。用例通过依赖覆盖把 LLM 换成
`NullLLMClient`，语义规则走降级路径，11 条规则的判定与整体等级完全确定。
真实 LLM 的通路由标 `llm` 的用例和 `demo_closed_loop.py`（默认走真实 Key）覆盖。

`expected_results.json` 的 AP-001 期望结果本身就是按"LLM 关闭时 R004/R009 为 `uncertain`、
仍是 `high`"校准的，两种模式都能跑通。

### 3.4 【R-04】演示脚本临时"补交附件"，跑完自动还原

AC17 要求"补齐附件后重试"。真实演示时，脚本把 `AP-002_服务合同.pdf` 复制成 AP-004 声明的
文件名（模拟审批人补交合同），重试完成后**自动删除**，恢复 AC16 的"附件缺失"基线——
否则样例集会被永久改坏，AC16 再也演示不出来。

### 3.5 【R-05】交互态截图：同源 iframe 夹具，用完即删

headless Chrome 的 `--screenshot` 只能截"加载完成时"的画面，点不出对话框。
为了给 FR-UI-08 留下一张"新增规则对话框"的真实截图，用一个临时同源 iframe 夹具
（`public/__shot.html`）载入调用端、轮询等按钮出现后 `click()`，再把整个 iframe 截下来。
夹具在截图后**立即删除**，不进交付物（否则会被打进 `dist/`）。

---

## 4. 验证记录

### 4.1 全量回归

```
$ uv run pytest -q          # 工作目录 backend/
287 passed, 2 warnings in 212.95s (0:03:32)
```

覆盖 TS-01…TS-20 全部场景；新增 `tests/test_m7_e2e.py` 8 例。

### 4.2 AC19 闭环实测（三进程真实运行）

```
$ uv run --project backend python scripts/demo_closed_loop.py --reset --all
```

三个进程：MySQL 容器、模拟审批系统（8100）、工具服务（8000）。**LLM 为真实调用**（`LLM=on`）。

```
工具服务 0.1.0｜MySQL 8.0.46 / charset=utf8mb4｜LLM=on
已清空业务表与附件目录
==============================================================================
AC19 端到端闭环演示：AP-001（全程无人工干预）
==============================================================================
① 拉取待办（IF-10）
  [OK] 拉到 5 条待办
  [OK] 任务 214：CG-2026-0001｜服务器采购合同审批｜张三
② 查看审批详情（IF-12 + IF-02）
  [OK] 审批单声明附件 1 个（AP-001_采购合同.pdf），本系统尚未下载
③④⑤ 下载附件 + 解析 + 16 字段提取（IF-14）
  [OK] parse_status=success / parse_mode=text / 字段记录 16 条
  [OK] 审批表单已缓存：contract_type=采购合同，form_data=5 项
  [OK] 附件落盘 AP-001_采购合同.pdf（8010 字节，SHA-256 f40883207d28…）
  [OK] 磁盘上确实存在 1 个文件：…\storage\contracts\214
⑥⑦ 规则审查 + 摘要/关注点（IF-16）
  [OK] 11 条规则全部给出判定
  [OK] 整体风险=high（命中 6，待确认 0）
  [OK] 命中项均含风险等级 + 证据 + 位置 + 建议：R001、R003、R005、R008、R010、R011
  [OK] 摘要 67 字 / 关注点 5 条（1–5 条）
⑧ 结果入库（IF-06 → DT-06 唯一）
  [OK] task_status=done / 重复审查不新增记录：review_id=195
⑨ 评论回写（IF-17 → DT-07 幂等）
  [OK] 回写成功 remark_id=RMK-88005
  [OK] 二次回写命中幂等键，不重复写评论
  [OK] 审批系统侧可读到 3 条评论
  [OK] 评论正文符合 §4.6.1 模板（标题 → 等级 → 摘要 → 关注点 → 免责声明）
补 全链路日志（IF-19，AC18）
  [OK] 核心日志类型齐备（7 类，无 ocr 属预期）
  [OK] 日志 14 条：download、extract、parse、pull、rule、save、write_comment
  [OK] 终态 task_status=done，write_status=success
==============================================================================
AC16 → AC17 异常阻塞与人工重试演示：AP-004
==============================================================================
① [OK] 审批单声明有附件 AP-004_办公用品采购合同.pdf，但文件确实不存在
② AC16
  [OK] 解析返回 HTTP 404 / 错误码 CONTRACT_ATTACHMENT_MISSING
  [OK] task_status=blocked，blocked_stage=parsing
③ [OK] 已补齐 AP-004_办公用品采购合同.pdf（10448 字节）
④ AC17
  [OK] 从 parsing 阶段重入 / task_status=done / retry_count=1
  [OK] 断点与错误码已清空（ST-01-02）
  [OK] 存在 log_type=retry 的日志（FR-LOG-05）
⑤ [OK] 审查 HTTP 200 / 回写成功 remark_id=RMK-88006
  （已删除临时附件 AP-004_办公用品采购合同.pdf，恢复 AC16 基线）
==============================================================================
演示结果：35/35 项判据通过
==============================================================================
```

> 说明：`待确认 0` 表示真实 LLM 对 R004/R009 给出了明确判定；LLM 关闭时这两条为
> `uncertain`，整体等级仍为 `high`（两种模式都跑通，见 §3.3）。

### 4.3 截图与前端验证

| 检查项 | 方式 | 结果 |
|---|---|---|
| 前端类型检查 | `npm run typecheck` | 通过（无输出） |
| 前端构建 | `npm run build` | 通过（1639 modules，11.22s） |
| 页面渲染断言 | headless Chrome `--dump-dom` + 关键内容匹配 | 七页全部命中（M6 记录 §4.3） |

`screenshots/` 共 12 张，全部为**真实运行的前后端**渲染所得（非手绘、非拼接）：

| 文件 | 对应 | 内容 |
|---|---|---|
| `AC01_待办拉取.png` | AC01 | 待办列表全字段 |
| `AC04_AP001解析结果.png` | AC04 | AP-001 的 8 + 8 条 FieldRecord |
| `AC05_AP003_OCR解析.png` | AC05 | `parse_mode=OCR 识别`、`共 1 页`、位置为 `第1页 区域(x,y)`、条款表 `缺失` 行橙色高亮 |
| `AC09_规则命中证据.png` | AC09 / AC10 | 整体风险「高」红块、命中 6 条、11 条规则明细含证据与建议 |
| `AC14_评论模板.png` | AC14 / AC15 | §4.6.1 模板评论 + 回写状态/时间/备注号 + 回写历史 |
| `AC16_AP004_blocked.png` | AC16 | 阻塞告警 + `CONTRACT_ATTACHMENT_MISSING` + 重试入口 |
| `AC17_人工重试.png` | AC17 | 重试后 `已完成` + `回写成功` + **重试次数 1** |
| `AC17_重试日志.png` | AC17 | 含 `人工重试` 类型的全链路日志 |
| `AC18_任务日志.png` | AC18 | 8 类覆盖条 + 全链路日志明细 |
| `AC19_端到端闭环.png` | AC19 | 闭环终态的审批信息 + 表单数据 + 已下载附件 |
| `FR-UI-03_missing高亮.png` | FR-UI-03 | 条款表 `missing` 行的视觉区分（长图） |
| `FR-UI-08_规则维护.png`、`FR-UI-08_新增规则对话框.png` | FR-UI-08 | 规则列表（启停/等级/建议）与新增对话框（交互态） |

---

## 5. 需要知悉的偏离与补充（请确认）

| 编号 | 内容 | 理由 |
|---|---|---|
| **M7-A** | `parse_task`（IF-14）现在会在解析前**尽力而为**地调用一次 IF-02 并缓存表单数据 | 修 §3.1 的缺陷；设计 §7 明确 `form_data_json` 是"PRD §6 第二步要求读取"。失败只记 warning，不改变任务状态，也不影响任何既有异常路径 |
| **M7-B** | 新增 `scripts/` 目录 | 设计 §13 的目录树未列"演示脚本"，但 §14 的 M7 要求"闭环演示"。放 `tests/` 会与断言型用例混淆，故单列 `scripts/` |
| **M7-C** | 前端的"表单数据"改从 IF-02 取（IF-12 作为回落） | 待办列表按 PRD 不含表单；IF-02 是审批侧的权威来源。IF-12 的 `form_data` 字段保留，用于审批系统不可达时展示缓存值 |
| **M7-D** | 提交物为 GitHub 仓库（不含离线压缩包）；提交文件夹命名按要求由你决定 | 你已确认"推送到 GitHub、不做离线压缩包" |

---

## 6. 交付清单与遗留问题

### 6.1 交付物（实战说明要求对照）

| 要求 | 落实 |
|---|---|
| 放代码文件（不带 `.venv` 依赖、模型文件） | `.gitignore` 排除 `.venv/`、`storage/`、`*.pdmodel`/`*.pdiparams`；提交前审计确认跟踪文件中无其一（§6.1 审计命令见下） |
| 放测试截图 | `screenshots/` 12 张，且在 `.gitignore` 中显式排除忽略 |
| 提交文件夹命名 | 按你的选择走仓库地址；命名规则由你决定 |
| 需求文档与规范 | `合同审批审查系统 PRD.md`、`docs/SPEC.md`、`docs/技术方案与设计.md`、`docs/架构图.md`、`docs/接口说明.md` |
| 里程碑证据 | `docs/M0-验收记录.md` … `docs/M7-验收记录.md` |
| 演示可复现 | `README.md` + `docs/演示说明.md` + `scripts/demo_closed_loop.py` |
| AC 可核对 | `docs/验收对照表.md`（AC01–AC19 → 用例 / 截图 / 判据） |

### 6.2 遗留问题（如实声明）

| # | 问题 | 影响 / 说明 |
|---|---|---|
| 1 | 未做多浏览器兼容性测试 | 仅在 Chrome 与 headless Chrome 实测 |
| 2 | 未做前端端到端自动化（Playwright/Cypress） | 见 M6-C；交互态截图用一次性 iframe 夹具生成 |
| 3 | 未做并发写冲突与压测 | 规则维护"后写覆盖"、长列表渲染性能均未压测 |
| 4 | OCR 单页约 60–77s（CPU） | AP-003 的完整闭环演示需等待；已有进度提示但无后台任务队列（MVP 串行处理，设计 §12 已声明） |
| 5 | LLM 判定存在波动 | R004/R009 依赖语义判断；`LLM_ENABLED=false` 时降级为 `uncertain`，两种模式的整体等级都稳定 |

### 6.3 未验证内容

- 未在**全新机器**上从零复现（当前验证机已装好 uv / Docker / Node 与 OCR 模型缓存）；
- 未验证 MySQL 8 以外的数据库版本；
- 未验证超过 200 条待办的分页与列表性能。
