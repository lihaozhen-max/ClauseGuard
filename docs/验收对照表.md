# ClauseGuard 验收对照表（AC01–AC19 → 证据索引）

> 用途：把 PRD 第 22 节 / SPEC §13 的 19 条验收标准逐条映射到**可复核的证据**——
> 自动化用例、实测脚本、截图、规范条款。
> 复查方式：`cd backend; uv run pytest -q`（287 项）→ 全绿即 AC01–AC19 的自动化部分全部成立。

| AC | 验证动作（SPEC §13） | 通过判据 | 自动化用例 | 其他证据 |
|---|---|---|---|---|
| **AC01** | 调 `IF-01(limit=5)` | 返回 ≥1 条待办，字段齐全 | `test_m1_pull.py`、`test_m7_e2e.py::test_ac19_full_closed_loop_on_ap001` | `screenshots/AC01_待办拉取.png` |
| **AC02** | 连续两次调 `IF-01` | 第二次 `created=0`/`updated=1`，`task_id` 不变，库中仅 1 条 | `test_m1_pull.py`、`test_m7_e2e.py::test_ac02_repull_is_idempotent` | 演示脚本 ① |
| **AC03** | 调 `IF-03` | 文件落盘、`file_size>0`、`file_checksum` 非空、DT-02 有记录 | `test_m2_attachment.py`、`test_m7_e2e.py`（回查磁盘） | 演示脚本 ③ |
| **AC04** | AP-001（文本型 PDF）调 `IF-04` | `parse_status=success`，16 字段全部产出 | `test_m2_parse.py`、`test_m7_e2e.py` | `screenshots/AC04_AP001解析结果.png` |
| **AC05** | AP-003（扫描件）调 `IF-04` | `parse_mode=ocr`，`full_text` 非空且含关键要素 | `test_m2_ocr.py`、`test_m5_logs.py::test_scanned_document_loop_covers_all_eight_log_types` | `screenshots/AC05_AP003_OCR解析.png`（位置为 `第1页 区域(x,y)`） |
| **AC06** | 查解析结果 | 8 基本信息 + 8 条款均有值或明确 `missing` | `test_m2_parse.py`、`test_m7_e2e.py` | `screenshots/FR-UI-03_missing高亮.png` |
| **AC07** | 抽查 3 个字段 | `source_text` 是 `full_text` 子串；`position` 符合 §2.6 正则 | `test_m2_parse.py`、`test_m7_e2e.py`（含 §2.6 正则断言） | — |
| **AC08** | 调 `IF-05` | 返回 §9 全部 11 条规则的判定 | `test_m3_rules_db.py`、`test_m7_e2e.py` | `screenshots/AC09_规则命中证据.png` |
| **AC09** | 检查命中项 | 每条命中含 `risk_level`+`evidence_text`+`evidence_position`+`suggestion` | `test_m3_predicates.py`、`test_m3_rules_db.py`、`test_m7_e2e.py` | `screenshots/AC09_规则命中证据.png` |
| **AC10** | 检查 AP-001 结果 | `overall_risk_level=high`；手算 RL-AGG 一致 | `test_m3_aggregator.py`、`test_m7_e2e.py` | `screenshots/AC09_规则命中证据.png`（「整体风险：高」） |
| **AC11** | 检查 `summary_text` | 非空、简体中文、提及主要风险 | `test_m4_summary.py`、`test_m7_e2e.py` | `screenshots/AC09_规则命中证据.png`（摘要块） |
| **AC12** | 检查 `focus_points` | 数组、1–5 条、每条 ≤40 字 | `test_m4_summary.py`、`test_m7_e2e.py` | `screenshots/AC09_规则命中证据.png`（关注点） |
| **AC13** | 调 `IF-06` | DT-06 有记录且 `task_id` 唯一；重复调用不新增 | `test_m4_review_db.py`、`test_m7_e2e.py` | 演示脚本 ⑧ |
| **AC14** | 检查 `comment_text` | 严格符合 §4.6.1 模板 | `test_m4_comment.py`、`test_m7_e2e.py`（含两种模板分支） | `screenshots/AC14_评论模板.png` |
| **AC15** | 调 `IF-07` | DT-07 记 `success` + `remark_id`；**审批系统侧出现该评论** | `test_m4_review_db.py`、`test_m7_e2e.py`（回读审批系统） | 演示脚本 ⑨（`审批系统侧可读到 N 条评论`） |
| **AC16** | AP-004（附件缺失）跑流程 | 任务 `blocked`、`blocked_stage=parsing`、`error_code=CONTRACT_ATTACHMENT_MISSING` | `test_m5_retry.py`、`test_m7_e2e.py` | `screenshots/AC16_AP004_blocked.png`、演示脚本 `--retry` ② |
| **AC17** | 调 `IF-20` 重试 AP-004（补齐附件后） | 回到 `parsing` 并最终 `done`，`retry_count=1`，有 `retry` 日志 | `test_m5_retry.py`、`test_m7_e2e.py` | `screenshots/AC17_人工重试.png`、`AC17_重试日志.png`、演示脚本 `--retry` ④ |
| **AC18** | 调 `IF-19` | 8 类 `log_type` 均有记录 | `test_m5_logs.py`、`test_m7_e2e.py::test_ac19_loop_leaves_full_task_log_trail` | `screenshots/AC18_任务日志.png` |
| **AC19** | 从待办拉取到评论回写完整跑一遍 AP-001 | 全链路无人工干预完成，各阶段数据齐全 | `test_m7_e2e.py`（8 例，含视图=库、可追踪链） | `screenshots/AC19_端到端闭环.png`、`scripts/demo_closed_loop.py`（35/35 判据） |

---

## 测试矩阵（TS-01…TS-20）落点

| TS | 场景 | 用例文件 |
|---|---|---|
| TS-01 | 待办拉取与去重（含并发双拉） | `test_m1_pull.py` |
| TS-02 | 附件下载成功 / 404 / 网络失败 | `test_m2_attachment.py` |
| TS-03 | 文本型 PDF 解析 | `test_m2_parse.py` |
| TS-04 | 扫描件 OCR 解析 | `test_m2_ocr.py` |
| TS-05 | 混合文档（text+ocr 页） | `test_m2_parse.py` |
| TS-06 | 空文档 / 损坏文件 | `test_m2_parse.py` |
| TS-07 | 16 字段提取完整性 | `test_m2_parse.py` |
| TS-08 | 证据子串校验（伪造证据必须被拒） | `test_m3_predicates.py`、`test_m3_llm.py` |
| TS-09 | RL-001…RL-011 每条规则的正例与反例 | `test_m3_predicates.py`（参数化） |
| TS-10 | RL-AGG 汇总（high/medium/low/空集/全 uncertain） | `test_m3_aggregator.py` |
| TS-11 | 评论模板渲染（有命中 / 无命中） | `test_m4_comment.py` |
| TS-12 | 回写幂等（重复调用） | `test_m4_review_db.py`、`test_m7_e2e.py` |
| TS-13 | 回写失败不删结果、任务保持 done | `test_m4_review_db.py` |
| TS-14 | 状态机非法迁移必须被拒绝 | `test_m1_pull.py`、`test_m5_retry.py` |
| TS-15 | 人工重试（parsing / reviewing 阶段） | `test_m5_retry.py` |
| TS-16 | LLM 降级（关 Key / 超时 / 空 content / 非法 JSON） | `test_m3_llm.py` |
| TS-17 | 日志脱敏（不得出现 Key/密码） | `test_m5_logs.py` |
| TS-18 | 内部接口鉴权（缺 `X-API-Key` → 401） | `test_m0_http.py`、`test_m5_logs.py`、`test_m6_rules_api.py` |
| TS-19 | 路径穿越附件名消毒 | `test_m2_attachment.py` |
| TS-20 | 闭环端到端（AC19） | `test_m7_e2e.py` |

## 非功能项

| 编号 | 要求 | 落点 |
|---|---|---|
| NF-06 | 密钥不得出现在日志/接口/截图中 | 脱敏过滤器 + `test_m5_logs.py`；`.env` 不入库（提交前审计） |
| NF-09 | 错误统一结构、不泄漏堆栈 | `app/core/errors.py` + `main.py` 的异常处理器 + `test_m0_http.py` |
| NF-10 | 风险等级三色视觉区分 | `frontend-or-client/src/styles.css`（`--cg-risk-*`）+ `screenshots/AC09_规则命中证据.png` |
| FR-SYS-02 | 附件禁止静态路由访问 | 后端不挂静态路由；前端详情页只展示元数据（`TaskDetailView.vue`） |
| FR-SYS-03 | 内部接口校验 `X-API-Key` | `app/core/security.py`（全部 `/api/**` 路由统一 `Depends`） |
| FR-LOG-03/04 | 日志脱敏 + 截断 ≤500 字符 | `app/core/logging.py`、`app/core/textutil.py` + `test_m5_logs.py` |

---

## 加固验证项（超出 SPEC 的 TS 矩阵，M7 追加轮）

| 编号 | 项 | 验证方式 | 结果 |
|---|---|---|---|
| H-01 | 并发回写同一评论不产生第二行、不变 500 | `test_m7_concurrency.py::test_concurrent_comment_write_keeps_single_row` | ✅（**修复前是 `IntegrityError` 500**） |
| H-02 | 并发回写收敛到 `success` | `test_m7_concurrency.py::test_concurrent_comment_write_converges_to_success` | ✅ |
| H-03 | 并发建同一 `rule_code` 只成功一次、其余 422 | `test_m7_concurrency.py::test_concurrent_rule_create_conflicts_are_business_errors` | ✅（**修复前是 `IntegrityError` 500**） |
| H-04 | 并发改同一规则的不同字段不丢更新 | `test_m7_concurrency.py::test_concurrent_rule_updates_do_not_lose_changes` | ✅ |
| H-05 | 6 路并发拉取不产生重复任务、无死锁 | `test_m7_concurrency.py::test_concurrent_pull_stays_single_task_per_instance` | ✅ |
| H-06 | 并发审查同一任务只留一份结果 | `test_m7_concurrency.py::test_concurrent_review_keeps_single_result_row` | ✅ |
| H-07 | FR-UI-01 待办列表与拉取按钮 | `test_m7_ui_e2e.py::test_task_list_renders_and_pull_button_works` | ✅ 真实点击 |
| H-08 | FR-UI-02/03/04/05/07 五个模块与三个动作按钮 | `test_m7_ui_e2e.py::test_task_pages_render_and_actions_work` | ✅ 真实点击 + NF-10 三色类名断言 |
| H-09 | FR-UI-06 `blocked` 重试入口 | `test_m7_ui_e2e.py::test_blocked_task_shows_retry_entry` | ✅ |
| H-10 | FR-UI-08 规则启停落库 | `test_m7_ui_e2e.py::test_rules_page_toggle_persists` | ✅ 刷新后仍生效 |
| H-11 | FR-SYS-03 密钥已注入、无对话框遮挡 | `test_m7_ui_e2e.py::test_no_api_key_dialog_blocks_nothing_when_key_present` | ✅ |
| H-12 | 性能基线可复现 | `scripts/bench.py` | ✅ 见 `docs/M7-验收记录.md` §7.3 |
| H-13 | 依赖层面从零复现 | `uv sync --frozen`（独立 venv）+ `npm ci` + 全量测试 + 构建指纹一致 | ✅ 见 §7.5 |
| H-14 | 新机器/新环境下"缺什么、怎么补"一条命令给结论 | `scripts/preflight.py`（只读、不打印密钥、退出码 0/1） | ✅ 22 项检查，见 §7.7 |
| H-15 | 前端大数据量渲染不退化 | `scripts/bench_ui.py`（真实 Chrome，5 → 1005 条任务） | ✅ 510ms → 406ms，见 §7.8 |
| H-16 | 窄屏（1024 / 800）不横向溢出、侧栏折叠 | `tests/test_m7_ui_e2e.py::test_narrow_viewport_has_no_horizontal_overflow`（参数化两档） | ✅ + 3 张窄屏截图，见 §7.9 |

> H-01/H-03 是本轮**由测试发现并修复的真实缺陷**，不是"补个用例"。
