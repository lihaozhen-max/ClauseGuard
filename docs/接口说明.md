# ClauseGuard · 接口说明

| 项 | 内容 |
|---|---|
| 文档编号 | CG-API-001 |
| 版本 | v1.0（M1 实现进度） |
| 依据 | `docs/SPEC.md` §5（IF-01…IF-21）、§5.3 错误结构、§5.4 错误码；PRD §15 |
| 更新日期 | 2026-09-17 |

---

## 1. 通用约定

### 1.1 鉴权

| 链路 | 请求头 | 配置项 |
|---|---|---|
| 调用端 → 工具服务 | `X-API-Key: {INTERNAL_API_KEY}` | CF-07 |
| 工具服务 → 模拟审批系统 | `X-API-Key: {APPROVAL_API_KEY}` | CF-06 |

缺失或不匹配 → `401`（工具服务返回 `UNAUTHORIZED`）。

### 1.2 时间格式

接口时间一律 ISO 8601 UTC，形如 `2026-09-10T02:30:00Z`（SPEC §2.3，由 `schemas/common.py` 统一转换）。

### 1.3 错误响应（SPEC §5.3）

```json
{
  "error": {
    "code": "APPROVAL_NOT_FOUND",
    "message": "任务 999 不存在",
    "task_id": 999,
    "detail": {}
  }
}
```

§5.4 的 12 个规范错误码见 `backend/app/core/errors.py`（含各自的 HTTP 状态与任务态影响）。
另有两个**框架级**补充码：`VALIDATION_ERROR`(422)、`INTERNAL_ERROR`(500)——为满足 NF-09「错误必须返回统一结构」而设。

### 1.4 实现进度

| 编号 | 状态 |
|---|---|
| IF-01、IF-02 | ✅ M1 |
| IF-03、IF-04 | ✅ M2 |
| IF-05 | ✅ M3 |
| IF-06、IF-07 | ✅ M4 |
| IF-10…IF-18 | ✅ M1/M2/M3/M4 |
| IF-19（任务日志）、IF-20（人工重试） | ⏳ M5 |
| IF-21（规则维护） | ⏳ M6 |

---

## 2. 工具接口（SPEC §5.1，签名与 PRD 一致，禁止改名改参数）

### IF-01 `list_pending_contract_approvals(limit)`

拉取待处理审批单并按 `instance_id` 幂等入库。

- **入参**：`limit: int`（默认 20，1 ≤ limit ≤ 200）
- **行为**：已存在则**更新**审批侧信息，绝不新建第二个任务；`task_status` 等任务态字段**不参与**更新（FR-APP-05）
- **返回**：

```json
{
  "items": [
    {
      "instance_id": "AP-001",
      "approval_code": "CG-2026-0001",
      "approval_title": "服务器采购合同审批",
      "applicant_name": "张三",
      "apply_time": "2026-09-10T02:30:00Z",
      "attachment_count": 1,
      "current_status": "pending",
      "task_id": 41,
      "task_status": "pending",
      "dedup": "created"
    }
  ],
  "created_count": 5,
  "updated_count": 0
}
```

- `dedup` ∈ `created` / `updated`
- **异常**：审批系统不可达/5xx → `APPROVAL_API_ERROR`(502)，`detail` 含 `method`、`url`、`error`

### IF-02 `get_contract_approval(instance_id)`

获取审批详情（审批信息 + 表单字段 + 合同类型 + 状态 + 附件列表），并附本系统任务映射。

```json
{
  "instance_id": "AP-001",
  "approval_code": "CG-2026-0001",
  "approval_title": "服务器采购合同审批",
  "applicant_name": "张三",
  "apply_time": "2026-09-10T02:30:00Z",
  "current_status": "pending",
  "contract_type": "采购合同",
  "form_data": { "amount": "500000", "currency": "CNY" },
  "attachments": [
    { "attachment_id": "ATT-001", "file_name": "AP-001_采购合同.pdf", "file_type": "pdf", "file_size": 8010 }
  ],
  "task_id": 41,
  "task_status": "pending"
}
```

- **异常**：实例不存在 → `APPROVAL_NOT_FOUND`(404)

> 该接口为**只读**，不修改本系统数据。

### IF-03 `download_contract_attachment(instance_id, attachment_id, file_name)`

按审批实例 + 附件编号下载合同附件，落盘到 `STORAGE_DIR/<task_id>/` 并登记 DT-02。

- **入参**：`instance_id: str`、`attachment_id: str`、`file_name: str | None`（缺省时用附件编号兜底）
- **落盘命名**：`{attachment_id}_{消毒后的文件名}`；文件名做路径穿越消毒（`..`、绝对路径、Windows 保留名、非法字符）
- **前置条件**：该审批实例必须已拉取为任务（IF-01），否则 `APPROVAL_NOT_FOUND`
- **返回**：

```json
{
  "task_id": 61,
  "attachment_id": "ATT-001",
  "file_name": "AP-001_采购合同.pdf",
  "file_type": "pdf",
  "file_path": "storage/contracts/61/ATT-001_AP-001_采购合同.pdf",
  "file_size": 8010,
  "file_checksum": "c35f62ef9d393e1b…",
  "download_status": "success"
}
```

> `file_path` 是**项目根相对路径**（不带 SPEC 示例中的 `./`），不随进程启动目录漂移；
> 附件**禁止**通过任何静态路由对外暴露（FR-SYS-02）。

- **异常**：附件不存在 → `CONTRACT_ATTACHMENT_MISSING`(404) + 任务 `blocked`/`parsing`；
  下载失败 → `DOWNLOAD_FAILED`(502) + 任务 `blocked`/`parsing`（`detail` 含 method/url/status_code/body）
- **幂等**：重复下载覆盖更新同一条 DT-02 记录（`UNIQUE(task_id, attachment_code)`）

### IF-04 `parse_contract_document(document_id)`

解析合同文件，产出 16 个字段（各一条 §2.4 的 5 键 FieldRecord）与统一合同全文。

- **入参**：`document_id: int`
  - 先按 `contract_parses.id` 解析；查不到再按 `approval_attachments.id` 解析（覆盖"首次解析尚无解析记录"的场景）
- **返回**：

```json
{
  "document_id": 23,
  "task_id": 61,
  "parse_mode": "text",
  "parse_status": "success",
  "basic_info": [
    {
      "field_name": "contract_amount",
      "field_value": "500000",
      "source_text": "本合同总金额为人民币伍拾万元整（¥500000.00）。",
      "position": "第1页 第10段",
      "extract_status": "success"
    }
  ],
  "clause_info": [
    {
      "field_name": "payment_clause",
      "field_value": "第三条 付款方式",
      "source_text": "第三条 付款方式\n1. 合同签订后三日内支付合同总额80%作为预付款。",
      "position": "第1页 第12段",
      "extract_status": "success"
    }
  ],
  "parse_error": null,
  "page_count": 2,
  "used_ocr_pages": [],
  "warnings": ["类型识别：魔数与扩展名一致"]
}
```

- **约束**：`basic_info` 与 `clause_info` **必须各含 8 条**（§2.5 全量），缺失者以 `extract_status=missing` 呈现，禁止缺项
- **`position` 格式**由 `parse_mode` 决定：`text` → `第N页 第M段`；`ocr` → `第N页 区域(x,y)`（§2.6）
- **`parse_status`**：存在 `failed` → `partial`；全部字段都取不到 → `failed`；其余 → `success`
  （`missing` 表示条款确实不存在，**不**算 `partial`）
- **异常**：内容为空 → `EMPTY_CONTRACT_CONTENT`(422)；OCR 失败 → `OCR_FAILED`(500)；
  解析失败 → `PARSE_FAILED`(500)；三者均置任务 `blocked`/`parsing`，并**必须**写入 `parse_error`
- **幂等**：重复解析同一附件更新原记录（`UNIQUE(task_id, attachment_id)`）

### IF-05 `run_contract_rules(case_id)`

执行全部 `rule_status=enabled` 的风险规则，返回命中、整体等级、摘要与关注点。

- **入参**：`case_id: int`（≡ `approval_tasks.id`）
- **返回**：

```json
{
  "case_id": 61,
  "overall_risk_level": "high",
  "hit_count": 6,
  "uncertain_count": 0,
  "evaluated_rules": 11,
  "rule_hits": [
    {
      "rule_code": "R001",
      "rule_name": "预付款比例风险",
      "risk_level": "high",
      "hit_status": "hit",
      "hit_source": "rule",
      "evidence_text": "1. 合同签订后三日内支付合同总额80%作为预付款。",
      "evidence_position": "第1页 第13段",
      "suggestion": "建议降低预付款比例或增加履约保障措施（如预付款保函）",
      "reason": "预付款比例 80% 超过阈值 30%"
    }
  ],
  "summary_text": "本合同共命中6项风险，其中高风险3项、中风险3项，主要涉及：…",
  "focus_points": ["建议降低预付款比例或增加履约保障措施（如预付款保函）"],
  "warnings": []
}
```

- **`rule_hits` 恒为 11 条**（R001–R011），`hit_status` ∈ `hit`/`miss`/`uncertain`；
  `hit_source` ∈ `rule`/`llm`（语义型规则由 LLM 判定时标 `llm`）
- **缺失型命中**（条款不存在，如 R008 / R010①）的 `evidence_text` 与 `evidence_position` 为 `null`——
  无原文可引，评论模板会改用"建议行"（§4.6.1）
- `evidence_text` 必为合同原文**连续子串**且 ≤300 字符（RL-00-02、PS-10）；`evidence_position` 合 §2.6
- `overall_risk_level` 由 **RL-AGG** 计算（存在 high → high；否则 medium → medium；否则 low）；
  `uncertain` 与 `miss` **不参与**计算（RL-AGG-01），且**禁止**由 LLM 决定（RL-AGG-03）
- **幂等**：重复执行按 `(task_id, rule_id)` 覆盖旧命中，不产生重复行（FR-RULE-07）
- **异常**：`case_id` 不存在 → `404 APPROVAL_NOT_FOUND`；无解析结果 → `409 PARSE_REQUIRED`
- **降级**：`LLM_ENABLED=false` / Key 为空 / 超时 / 空 `content` / 非法 JSON 时，
  语义型规则退化为关键词与存在性判定，无法判定者记 `uncertain`，
  任务**不进入 `blocked`**（LM-14 / LM-15 / LM-18）

### IF-06 `save_review_result(case_id, overall_risk_level, summary_text, focus_points_json, comment_text)`

保存审查结果（按 `task_id` upsert，`UNIQUE(task_id)` 是最终保障）。

- **入参**：`focus_points_json` 接受 JSON 字符串或字符串数组（PRD 签名沿用 json 字样）；
  非法 JSON → `422 VALIDATION_ERROR`
- **返回**：`{ "review_id": 1, "task_id": 1, "overall_risk_level": "high", "summary_text": "…",
  "focus_points": ["…"], "comment_text": "…", "summary_degraded": false }`
- **幂等**：重复调用**更新同一 `review_id`**，不新增记录（FR-REV-05 / AC13）
- **副作用**：保存成功后任务 `reviewing → done`（ST-01）
- 内部 REST 未单列本接口：`POST /api/tasks/{id}/review` 已把它并入完整审查流程（见 IF-16）

### IF-07 `write_approval_comment(instance_id, review_id)`

把已保存的审查结果写入审批系统评论区（幂等）。

- **返回**：

```json
{
  "task_id": 102,
  "review_id": 35,
  "write_status": "success",
  "remark_id": "RMK-88001",
  "write_response_text": "{\"code\":0,\"remark_id\":\"RMK-88001\",\"duplicate\":false}",
  "duplicate": false
}
```

- **幂等**：`idempotency_key = SHA-256(instance_id + ':' + review_id)`（DT-07 ⭐UNIQUE）。
  已 `success` 的记录再次调用**直接返回既有结果**（`duplicate=true`），
  **不再请求审批系统**（FR-COM-06 / ST-02-01）
- **状态**：`not_written → writing → success|failed`；`failed` 可重新触发（ST-02）
- **失败**：`COMMENT_WRITE_FAILED`(502) —— 仅置 `write_status=failed`，
  任务**保持 `done`**、已产生的审查结果**不删除**（FR-COM-04 / ST-01-04）
- **守卫**：正文必须以免责声明结尾且不得含替代人工决策的表述，否则拒绝回写（FR-COM-07）
- **异常**：实例或审查结果不存在、或 `review_id` 不属于该实例 → `404 APPROVAL_NOT_FOUND`

---

## 3. 内部 REST（SPEC §5.2，调用端 → 工具服务）

统一前缀 `/api`，全部要求 `X-API-Key`。

### IF-10 `POST /api/tasks/pull`

触发待办拉取。请求体可省略。

```json
{ "limit": 20 }
```

返回同 IF-01。`limit` 越界（如 0）→ `422 VALIDATION_ERROR`。

### IF-11 `GET /api/tasks?status=&page=&size=`

任务列表（分页）。

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `status` | `pending`/`parsing`/`reviewing`/`blocked`/`done` | 无 | 非法值 → 422 |
| `page` | int ≥ 1 | 1 | |
| `size` | int 1–200 | 20 | |

```json
{
  "items": [
    {
      "task_id": 41,
      "instance_id": "AP-001",
      "approval_code": "CG-2026-0001",
      "approval_title": "服务器采购合同审批",
      "applicant_name": "张三",
      "apply_time": "2026-09-10T02:30:00Z",
      "attachment_count": 1,
      "current_status": "pending",
      "task_status": "pending",
      "write_status": "not_written"
    }
  ],
  "total": 5,
  "page": 1,
  "size": 20
}
```

字段与 FR-UI-01 的展示需求一一对应。按 `task_id` 倒序。

### IF-12 `GET /api/tasks/{task_id}`

任务详情（含附件），**全部来自本系统数据库**（不依赖审批系统可用性）。

```json
{
  "task_id": 41,
  "instance_id": "AP-001",
  "approval_code": "CG-2026-0001",
  "approval_title": "服务器采购合同审批",
  "applicant_name": "张三",
  "apply_time": "2026-09-10T02:30:00Z",
  "attachment_count": 1,
  "current_status": "pending",
  "contract_type": "采购合同",
  "form_data": { "amount": "500000" },
  "task_status": "pending",
  "write_status": "not_written",
  "blocked_stage": null,
  "error_code": null,
  "error_message": null,
  "retry_count": 0,
  "created_at": "2026-09-17T09:03:03Z",
  "updated_at": "2026-09-17T09:03:03Z",
  "attachments": []
}
```

- `attachments` 元素：`{attachment_id, file_name, file_type, file_size, file_checksum, download_status}`
  （`attachment_id` 对外用审批系统侧编号，内部存 `approval_attachments.attachment_code`）
- 尚未下载附件（未跑过 IF-03 / IF-14）时 `attachments` 为空数组——这是正常状态，不是错误
- **异常**：任务不存在 → `404` + `APPROVAL_NOT_FOUND`（`detail.task_id` 给出请求的 id）

> SPEC §5.4 的 12 个规范错误码未单列"任务不存在"，而 IF-05 对不存在的 `case_id` 同样只要求 404，
> 故此处沿用 404 + `APPROVAL_NOT_FOUND`，不新增业务错误码。

### 辅助 `GET /api/approvals/{instance_id}`

IF-02 的 REST 形态（SPEC 内网路由表未列，但调用端详情页需要）。语义与工具接口完全一致。

### IF-13 `GET /api/tasks/{task_id}/parse`

查询解析结果（形状同 IF-04 的返回）。

- **异常**：任务不存在 → `404 APPROVAL_NOT_FOUND`；**尚未解析 → `409 PARSE_REQUIRED`**
- 解析失败时返回 **200 + `parse_status=failed` + `parse_error`**：失败原因对排查有用，不应藏起来
  （FR-PARSE-07 要求必须记录 `parse_error`）

### IF-14 `POST /api/tasks/{task_id}/parse`

触发"下载附件 + 解析"流水线（内部编排 IF-03 + IF-04），返回主文档的解析结果。

- 库中已有附件记录则直接解析；否则先从审批系统详情取附件清单并逐个下载
- **多附件**时解析全部，返回第一个附件的结果；IF-13 查询时取最新一条解析记录
- **异常**：同 IF-03 / IF-04（附件缺失、内容为空、OCR 失败、解析失败）

### IF-15 `GET /api/tasks/{task_id}/review`

查询**已保存**的审查结果（规则命中 + 整体等级 + 摘要 + 关注点 + 评论正文）。

- 返回结构同 IF-16（`ReviewPipelineResponse`）：`review_id`、`overall_risk_level`、`hit_count`、
  `uncertain_count`、`evaluated_rules`、`rule_hits`（恒 11 条）、`summary_text`、`focus_points`、
  `comment_text`、`task_status`、`write_status`
- **异常**：任务不存在 → `404 APPROVAL_NOT_FOUND`；**尚未执行审查 → `409 PARSE_REQUIRED`**
  （消息会提示先调用 `POST /api/tasks/{task_id}/review`）

### IF-16 `POST /api/tasks/{task_id}/review`

触发**完整审查**：`IF-05 规则判定` → 摘要/关注点生成（LLM，失败回落模板）→
`§4.6.1 评论渲染` → `IF-06 落库` → 任务 `reviewing → done`。

- 返回结构同 IF-15，另含 `comment_text`（即将写入评论区的正文）与
  `summary_degraded`（摘要是否走了 §10.4 模板降级）
- LLM 客户端经依赖注入（`get_llm_client`），生产为进程级单例，测试可注入桩
- **异常**：未解析 → `409 PARSE_REQUIRED`；任务不存在 → `404`

### IF-17 `POST /api/tasks/{task_id}/write-comment`

触发或重试评论回写（内部调用 IF-07，幂等）。返回结构同 IF-07。

- 尚未生成审查结果 → `409 PARSE_REQUIRED`
- 回写失败 → `502 COMMENT_WRITE_FAILED`，任务保持 `done`

### IF-18 `GET /api/tasks/{task_id}/comment-logs`

查询回写状态与历史（DT-07）。

```json
{
  "items": [
    {
      "id": 1,
      "task_id": 102,
      "write_status": "success",
      "write_response_text": "{\"code\":0,\"remark_id\":\"RMK-88001\",...}",
      "remark_id": "RMK-88001",
      "idempotency_key": "7f1e100a…",
      "created_at": "2026-09-18T02:02:55Z"
    }
  ],
  "total": 1
}
```

> 每个 `(instance_id, review_id)` 只有**一条**记录（唯一索引即幂等保障）；
> 每次回写**尝试**的完整轨迹记在 `task_logs`（`log_type=write_comment`）。

### 系统 `/health`

无需鉴权。返回服务版本、环境、数据库连通性与 LLM 是否降级。数据库异常信息已脱敏。

---

## 4. 模拟审批系统接口（外部系统，SPEC FR-MOCK-01…FR-MOCK-04）

基址 `APPROVAL_BASE_URL`（默认 `http://127.0.0.1:8100`）。

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/approvals/pending?limit=` | 待办列表（按 `apply_time` 升序） |
| `GET` | `/approvals/{instance_id}` | 审批详情；不存在 → 404 |
| `GET` | `/approvals/{instance_id}/attachments/{attachment_id}/download` | 附件**真实文件流**；附件不存在 → 404（AP-004 走此分支） |
| `POST` | `/approvals/{instance_id}/comments` | 评论写入，按 `idempotency_key` 去重，返回 `remark_id` |
| `GET` | `/approvals/{instance_id}/comments` | **辅助**接口：回读已写入评论（供 AC15 核对） |
| `GET` | `/health` | 健康检查，含 `missing_attachment_files` |

评论写入请求 / 响应：

```json
// 请求
{ "idempotency_key": "a3f1…", "content": "【合同自动审查结果】…" }
// 首次
{ "code": 0, "remark_id": "RMK-88001", "duplicate": false }
// 同键重复
{ "code": 0, "remark_id": "RMK-88001", "duplicate": true }
```

错误体（外部系统自有格式，非工具服务的 §5.3 结构）：

```json
{ "detail": { "code": 404, "message": "附件 ATT-004（AP-004_办公用品采购合同.pdf）不存在" } }
```
