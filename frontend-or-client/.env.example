# 调用端环境变量模板（复制为 .env.local，已被 .gitignore 忽略）
#
# 说明：`X-API-Key` 必须与后端 ClauseGuard/.env 的 INTERNAL_API_KEY 一致（FR-SYS-03）。
# 这里留空也可以——页面右上角"接口密钥"对话框可临时填写，
# 存在浏览器 localStorage 里，方便演示时无需重新构建。
#
# 注意：调用端跑在浏览器里，密钥对使用者必然可见；这是内网工具的正常形态，
# 真正的边界是"仅内网可达 + 后端校验"，不是"前端藏密钥"。

VITE_INTERNAL_API_KEY=
